namespace CodeContractor.Contracts.Assertions
{
    /// <summary>
    /// Represents a list of supported assertions by Code Contract library.
    /// </summary>
    public enum CodeContractAssertionType
    {
        Requires,
        Ensures,
        Invariant,
        Assert,
        Assume,
    }
}